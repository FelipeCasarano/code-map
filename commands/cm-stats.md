---
description: Show the per-session Code Map token-savings ledger.
---

Read the current session ledger:

!`node ${CLAUDE_PLUGIN_ROOT}/src/cli/index.js stats --json`

Summarize `events`, `raw_candidate_tokens`, `delivered_tokens`, and `saved_percent` for the user.
