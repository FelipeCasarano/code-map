{
  "note": "This directory holds the local Code Map index. Everything except template.json is generated by `cm sync` and is gitignored.",
  "files": {
    "index.json": "file-level metadata keyed by relative path",
    "symbols.jsonl": "one symbol per line: function/class/route/test",
    "summaries.jsonl": "short anchor-derived summaries per file",
    "aliases.jsonl": "resolver hints and common misspellings",
    "benchmark.json": "latest benchmark snapshot",
    "version.json": "index schema + repo fingerprint",
    "sessions/": "per-session cm_stats ledgers"
  }
}
